<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wanghui.shiyue.article.dao.CatalogMapper">

    <!-- 根据父目录id查询子目录 -->
    <select id="selectByParentId" resultType="com.wanghui.shiyue.article.entity.po.CatalogPO">
        select * from web.catalog where parent_id = #{parentId} and invalid_flag = '0' order by catalog_id
    </select>

    <!-- 查询所有目录，按层级和路径排序 -->
    <select id="selectAllOrdered" resultType="com.wanghui.shiyue.article.entity.po.CatalogPO">
        select * from web.catalog where invalid_flag = '0' order by level, path
    </select>

    <!-- 根据目录路径查询目录 -->
    <select id="selectByPath" resultType="com.wanghui.shiyue.article.entity.po.CatalogPO">
        select * from web.catalog where path = #{path} and invalid_flag = '0'
    </select>

    <!-- 根据目录层级查询目录 -->
    <select id="selectByLevel" resultType="com.wanghui.shiyue.article.entity.po.CatalogPO">
        select * from web.catalog where level = #{level} and invalid_flag = '0' order by catalog_id
    </select>

</mapper>
